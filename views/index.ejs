<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Smart Inventory Control - Welcome</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    @import url('http://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap');

    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: 'Poppins', sans-serif;
    }

    body {
      height: 100vh;
      background: linear-gradient(135deg, #6a11cb, #2575fc);
      display: flex;
      justify-content: center;
      align-items: center;
      text-align: center;
      overflow: hidden;
      animation: bg-animation 8s ease-in-out infinite alternate;
    }

    @keyframes bg-animation {
      0% { background: linear-gradient(135deg, #6a11cb, #2575fc); }
      100% {  background: linear-gradient(135deg, #6a11cb, #2575fc); }
    }

    .container {
      background: rgba(255, 255, 255, 0.15);
      backdrop-filter: blur(12px);
      padding: 3rem 4rem;
      border-radius: 20px;
      box-shadow: 0 15px 40px rgba(0,0,0,0.2);
      color: white;
      max-width: 800px;
      width: 90%;
      animation: fadeIn 1.2s ease-in-out;
    }

    @keyframes fadeIn {
      0% { opacity: 0; transform: translateY(30px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    h1 {
      font-size: 3rem;
      font-weight: 700;
      margin-bottom: 1rem;
    }

    p {
      font-size: 1.2rem;
      color: #f1f1f1;
      margin-bottom: 2.5rem;
      line-height: 1.6;
    }

    .btn-group {
      display: flex;
      justify-content: center;
      gap: 2rem;
      flex-wrap: wrap;
    }

    a.btn {
      text-decoration: none;
      padding: 1rem 3rem;
      border-radius: 50px;
      font-weight: 700;
      font-size: 1.2rem;
      letter-spacing: 1.2px;
      transition: all 0.3s ease;
      background: linear-gradient(135deg, #43e97b, #38f9d7);
      color: white;
      box-shadow: 0 8px 20px rgba(56,249,215,0.4);
      border: 2px solid transparent;
      display: inline-block;
    }

    a.btn:hover,
    a.btn:focus {
      background: linear-gradient(135deg, #38f9d7, #43e97b);
      transform: scale(1.05);
      outline: none;
    }

    @media (max-width: 600px) {
      .container {
        padding: 2rem;
      }
      h1 {
        font-size: 2.2rem;
      }
      p {
        font-size: 1rem;
      }
      .btn-group {
        flex-direction: column;
        gap: 1.2rem;
      }
      a.btn {
        font-size: 1.1rem;
        padding: 0.9rem 2rem;
      }
    }
  </style>
</head>
<body>
  <main class="container">
    <h1>Welcome to Smart Inventory Control</h1>
    <p>
      Manage your raw materials and finished products with real-time tracking, low-stock alerts, and seamless control.  
      Start now and make your inventory smarter!
    </p>

    <div class="btn-group">
      <a href="/login" class="btn">Login</a>
      <a href="/signup" class="btn">Sign Up</a>
    </div>
  </main>
</body>
</html>
